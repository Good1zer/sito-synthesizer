If any rule fails, you must:

1. Fix ONLY the violating part
2. Do NOT rewrite unrelated layout
3. Do NOT refactor working code
4. Show BEFORE / AFTER code
5. Re-run validation after fix

Repeat until ALL rules = PASS.

---

## IMPORTANT

* Do NOT introduce new layout violations while fixing
* Do NOT replace flow layout with alternative approaches
* Do NOT "improve" code that already passes validation
